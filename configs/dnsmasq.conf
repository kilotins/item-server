# Item Consulting internal DNS
# All *.item.lan resolves to this server

# Wildcard for internal domain
address=/item.lan/192.168.1.100

# Don't forward .intern queries upstream
local=/item.lan/

# Use upstream DNS for everything else
server=8.8.8.8
server=1.1.1.1
